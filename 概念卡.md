# 概念卡

> **它解决什么问题**：上课「听不懂」。
> 原因往往不是理解力，而是**课从第一分钟起就假设你认识一批名词**——不认识名词，后面全是噪音（就像没学过"导数"去听微积分）。
> 所以顺序必须反过来：**先补名词，再上课。**

## 怎么用（4 条规则）

1. 一遇到不认识的词，**立刻停下来**记它，别硬听下去。
2. 每个词只记三样：**一句话说明它是什么 + 3 行能跑的代码 + 一个我自己举的例子**。不背定义。
3. **先跑再看**：那 3 行代码一定亲手敲进 Python 跑一遍，看着输出写卡片。"先会用，后理解"是唯一有效的顺序。
4. 同一个词记到**第 3 次还不懂** → 它是**前置缺口**，不是笨：标上「待补」，退回去做更小的练习，**不许硬啃**。

## 我的概念卡

> 下面三行是**格式示例**（用 2026-09-20 真实踩过的坑写的）。看懂格式之后删掉它们，换成自己的。

| 概念 | 一句话（我自己写的） | 3 行能跑的例子 | 第几次 / 日期 |
|---|---|---|---|
| `Path` | 一个"路径对象"，专门用来处理文件路径，比纯字符串聪明 | `from pathlib import Path`<br>`p = Path(r"E:\Practice_Forms.xlsx")`<br>`print(p.stem, p.suffix)` | 1次 · 2026-09-20 |
| `header` | 告诉 pandas「第几行是表头」，**从 0 开始数** | `import pandas as pd`<br>`d = pd.read_excel("x.xlsx", header=1)`<br>`print(list(d.columns))` | 1次 · 2026-09-20 |
| `encoding` | 告诉程序用哪种字符编码去读写文件 | `f = open("a.csv", "w", encoding="utf-8-sig")`<br>`f.write("中文")`<br>`f.close()  # 不加 utf-8-sig，Excel 打开会乱码` | 1次 · 2026-09-20 |
|  |  |  |  |
|  |  |  |  |
|  |  |  |  |

## 待补清单（先查这批词，再去看课）

- **UCB CS61A 前 5 讲会直接出现**：`expression` `value` `type` `operator` `name` `assignment` `function` `argument` `parameter` `return` `docstring` `doctest` `higher-order function` `lambda` `environment` `frame` `scope`
- **MIT Missing Semester 第 1–2 讲会直接出现**：`shell` `prompt` `PATH` `executable` `flag / 参数` `标准输入输出` `重定向` `管道` `通配符` `退出码` `环境变量` `alias`

## 复习节奏（配合「间隔重复」原则）

记完一张卡，在 **Day 1 / 3 / 7 / 15 / 30** 各回来看一次：

> **只看「概念」那一格，能不能自己说出另外两格？**
> 能 → 这次复习算过；不能 → 不算过，明天再来。

[← 回到仓库首页](README.md) ｜ [卡点日志 →](卡点日志.md)
